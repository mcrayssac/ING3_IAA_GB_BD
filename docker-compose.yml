# Cluster profile of the configuration table in docs/architecture.md.
# Values come from .env, which is not committed. See .env.example.
x-cluster-env: &cluster-env
    SPARK_MASTER: spark://spark-master:7077
    S3_ENDPOINT: http://rustfs:9000
    S3_ACCESS_KEY: ${S3_ACCESS_KEY:-rustfsadmin}
    S3_SECRET_KEY: ${S3_SECRET_KEY:-rustfsadmin}
    S3_BUCKET: ${S3_BUCKET:-nyc-taxi}
    PG_HOST: postgres
    PG_PORT: "5432"
    PG_DATABASE: ${PG_DATABASE:-nyc_taxi}
    PG_USER: ${PG_USER:-nyc_taxi}
    PG_PASSWORD: ${PG_PASSWORD:?set PG_PASSWORD in .env, see .env.example}
    PG_SCHEMA: ${PG_SCHEMA:-dw}

services:

    rustfs:
        image: rustfs/rustfs:latest
        container_name: rustfs
        hostname: rustfs
        ports:
                - "9000:9000"  # S3 API
                - "9001:9001"  # Web console
        environment:
                - RUSTFS_ADDRESS=0.0.0.0:9000
                - RUSTFS_CONSOLE_ADDRESS=0.0.0.0:9001
                - RUSTFS_CONSOLE_ENABLE=true
                - RUSTFS_ACCESS_KEY=${S3_ACCESS_KEY:-rustfsadmin}
                - RUSTFS_SECRET_KEY=${S3_SECRET_KEY:-rustfsadmin}
                - RUSTFS_VOLUMES=/data
        volumes:
                - rustfs-data:/data
        networks:
                - spark-net

    postgres:
        image: postgres:18-alpine
        container_name: postgres
        hostname: postgres
        ports:
            - "5432:5432"
        environment:
            POSTGRES_DB: ${PG_DATABASE:-nyc_taxi}
            POSTGRES_USER: ${PG_USER:-nyc_taxi}
            POSTGRES_PASSWORD: ${PG_PASSWORD:?set PG_PASSWORD in .env, see .env.example}
        volumes:
            - postgres-data:/var/lib/postgresql
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U ${PG_USER:-nyc_taxi} -d ${PG_DATABASE:-nyc_taxi}"]
            interval: 10s
            timeout: 5s
            retries: 5
        networks:
            - spark-net

    spark-master:
        image: apache/spark:4.2.0
        container_name: spark-master
        hostname: spark-master
        ports:
            - "8080:8080"
            - "7077:7077"
        environment:
            <<: *cluster-env
            SPARK_NO_DAEMONIZE: "true"
        volumes:
            - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
        entrypoint: ["/opt/spark/bin/spark-class"]
        command: ["org.apache.spark.deploy.master.Master"]
        networks:
            - spark-net

    spark-worker-1:
        image: apache/spark:4.2.0
        container_name: spark-worker-1
        hostname: spark-worker-1
        depends_on:
            - spark-master
        ports:
            - "8081:8081"
        environment:
            <<: *cluster-env
            SPARK_NO_DAEMONIZE: "true"
            SPARK_WORKER_CORES: "2"
            SPARK_WORKER_MEMORY: 2g
        volumes:
            - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
        entrypoint: ["/opt/spark/bin/spark-class"]
        command: ["org.apache.spark.deploy.worker.Worker", "spark://spark-master:7077"]
        networks:
            - spark-net

    spark-worker-2:
        image: apache/spark:4.2.0
        container_name: spark-worker-2
        hostname: spark-worker-2
        depends_on:
            - spark-master
        ports:
            - "8082:8081"
        environment:
            <<: *cluster-env
            SPARK_NO_DAEMONIZE: "true"
            SPARK_WORKER_CORES: "2"
            SPARK_WORKER_MEMORY: 2g
        volumes:
            - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
        entrypoint: ["/opt/spark/bin/spark-class"]
        command: ["org.apache.spark.deploy.worker.Worker", "spark://spark-master:7077"]
        networks:
            - spark-net

networks:
    spark-net:
        driver: bridge

volumes:
    rustfs-data:
    postgres-data:
